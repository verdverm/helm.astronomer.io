apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "airflow-operator.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "airflow-operator.name" . }}
    helm.sh/chart: {{ include "airflow-operator.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "airflow-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
